<%= form_for(@web_seed) do |f| %>
  <% if @web_seed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_seed.errors.count, "error") %> prohibited this web_seed from being saved:</h2>

      <ul>
      <% @web_seed.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_area :url %>
  </div>
  <div class="field">
    <%= f.label :votes %><br />
    <%= f.number_field :votes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
