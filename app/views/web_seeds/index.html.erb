<h1>Listing web_seeds</h1>

<table>
  <tr>
    <th>Url</th>
    <th>Votes</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @web_seeds.each do |web_seed| %>
  <tr>
    <td><%= web_seed.url %></td>
    <td><%= web_seed.votes %></td>
    <td><%= link_to 'Show', web_seed %></td>
    <td><%= link_to 'Edit', edit_web_seed_path(web_seed) %></td>
    <td><%= link_to 'Destroy', web_seed, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Web seed', new_web_seed_path %>
